name: smoke

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: compilers
        run: sudo apt-get update && sudo apt-get install -y gcc g++ clang make python3
      - name: smoke
        run: bash scripts/smoke.sh
